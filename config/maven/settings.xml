<settings>
  <mirrors>
    <!-- http://localhost:8081/nexus/content/groups/public (when port forwarding to build-server (si) ) -->
    <mirror>
      <!--This sends everything else to /public -->
      <id>nexus</id>
      <mirrorOf>*</mirrorOf>
      <url>http://build.imperatives.co.uk:8081/nexus/content/groups/public</url>
    </mirror>
  </mirrors>
</settings>
